# 从输入 URL 到页面展示的完整过程

这是一个经典的问题，涉及网络、浏览器架构、渲染引擎等多个层面。我按时间顺序展开讲。

## 一、URL 解析阶段

浏览器首先解析你输入的字符串，判断这是一个合法 URL 还是搜索关键词。如果是 URL，会拆解出：
- **协议**（http/https）
- **域名**
- **端口**（默认 80/443）
- **路径、查询参数、锚点**

如果没写协议，浏览器通常会先尝试 HTTPS（现代浏览器策略），或者根据 HSTS 列表强制升级。

## 二、DNS 解析

需要把域名转换成 IP 地址，查找顺序一般是：

1. **浏览器 DNS 缓存**
2. **操作系统 DNS 缓存**
3. **本地 hosts 文件**
4. **路由器缓存**
5. **ISP 的 DNS 服务器**（递归查询）
6. 如果都没有，走完整的**递归查询**：根域名服务器 → 顶级域名服务器（.com）→ 权威域名服务器，逐级返回 IP

现代场景下还可能涉及 **DNS-over-HTTPS/TLS**、**CDN 智能解析**（返回离用户最近的节点 IP）。

## 三、建立 TCP 连接

拿到 IP 后，浏览器与服务器建立 TCP 连接，即经典的**三次握手**：

1. 客户端发 SYN
2. 服务器回 SYN+ACK
3. 客户端回 ACK

连接建立后才能传输数据。现代浏览器还会做 **TCP 预连接**（如 DNS prefetch、preconnect）来优化。

## 四、TLS 握手（如果是 HTTPS）

在 TCP 之上，还需要建立安全通道：

1. 客户端发送 Client Hello（支持的加密套件、TLS 版本等）
2. 服务器返回 Server Hello + 证书
3. 客户端验证证书合法性（CA 签名链、有效期、域名匹配）
4. 双方协商生成会话密钥（TLS 1.3 已经把握手简化为 1-RTT）

握手完成后，后续通信都通过对称加密传输。

## 五、发送 HTTP 请求

连接建立后，浏览器构造 HTTP 请求报文发送给服务器，包含：

- 请求行（方法、路径、协议版本）
- 请求头（Host、User-Agent、Cookie、Accept-Encoding 等）
- 请求体（如果是 POST）

这里会涉及**缓存判断**：如果本地有强缓存（Cache-Control/Expires）且未过期，浏览器可能直接不发请求，用本地资源；如果有协商缓存（ETag/Last-Modified），会带上 If-None-Match 等头，服务器判断后返回 304。

## 六、服务器处理请求

请求到达服务器（可能先经过负载均衡、反向代理如 Nginx、CDN 边缘节点），然后：

- 路由匹配
- 业务逻辑处理（可能查数据库、调用其他服务）
- 渲染模板或返回 JSON（取决于 SSR 还是 CSR）
- 组装 HTTP 响应（状态码、响应头、响应体）

## 七、浏览器接收响应

浏览器拿到响应后，先看状态码：
- **3xx** 重定向：重新发起请求
- **4xx/5xx** 错误：走错误页面
- **2xx** 成功：进入渲染阶段

## 八、浏览器渲染流程（核心部分）

这里是最值得展开的部分，涉及**关键渲染路径（Critical Rendering Path）**：

### 1. 构建 DOM 树
浏览器逐字节解析 HTML，遇到标签生成对应的 DOM 节点，构建成树状结构。这个过程是**流式**的，边下载边解析。

### 2. 构建 CSSOM 树
解析 CSS（内联、内部、外部样式表），生成 CSSOM。CSS 是**渲染阻塞资源**——因为任何元素的最终样式都可能被后面的规则覆盖，所以必须等 CSSOM 构建完成才能往下走。

### 3. JavaScript 的介入
- 遇到 `<script>` 标签默认会**阻塞 HTML 解析**，因为 JS 可能修改 DOM
- 如果脚本引用外部文件，还要等下载完成
- `async`：下载不阻塞解析，下载完立即执行（执行时机不确定）
- `defer`：下载不阻塞解析，等 DOM 解析完成后按顺序执行
- JS 执行如果需要访问 CSSOM（比如 `getComputedStyle`），会等 CSSOM 就绪，这就是所谓 **JS 阻塞在 CSS 后面**的情况

### 4. 合并成渲染树（Render Tree）
DOM + CSSOM 合并，只包含**需要显示的节点**（`display: none` 的节点不在其中，但 `visibility: hidden` 的节点在）。

### 5. 布局（Layout / Reflow）
计算每个渲染树节点在屏幕上的**精确位置和大小**（盒模型计算）。

### 6. 绘制（Paint）
把每个节点转换成屏幕上的实际像素——填色、文字、边框、阴影等。

### 7. 合成（Composite）
现代浏览器会把页面拆分成多个图层（Layer），分别栅格化后交给 GPU 合成，这样可以让某些动画（如 transform、opacity）不用重新走布局和绘制，性能更好。

## 九、异步资源加载

图片、字体、异步脚本、iframe 等资源会并行加载，加载完成后可能触发**重排（reflow）或重绘（repaint）**，页面逐步"长出来"。

浏览器对同一域名的并发连接数有限制（一般 6 个），所以现代方案常用 **HTTP/2 多路复用**或 **CDN 域名分散**来提升并行度。

## 十、关键事件节点

- `DOMContentLoaded`：DOM 树构建完成（不含图片等资源）
- `load`：所有资源（图片、样式、脚本）加载完成

## 十一、用户可交互 & 后续

页面渲染完成后，用户可以滚动、点击、输入，触发 JS 事件监听、可能有懒加载、埋点上报等。

## 十二、连接关闭

数据传输完成后，TCP 连接通过**四次挥手**关闭（不过 HTTP/1.1 的 keep-alive、HTTP/2 的连接复用，会让连接保持更久以减少重复握手开销）。

---
