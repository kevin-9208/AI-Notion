整个前端生态工具的关系：

浏览器
    │
    ▼
HTML / CSS / JavaScript
    │
    ▼
ES Modules（模块化）
    │
    ▼
工程化工具
    │
    ├── Webpack（成熟、生态庞大）
    ├── Vite（开发体验优秀）
    ├── Rollup（库打包）
    ├── Parcel（零配置）
    ├── Rspack（Webpack 兼容 + Rust）
    ├── esbuild（极速构建引擎）
    └── SWC（极速编译器）

这张图其实画出的是前端工程化过去十年的演进主线,但里面藏着好几层容易混淆的关系(打包器 vs 编译器、JS 实现 vs 系统语言实现、开发时 vs 生产时)。展开讲讲。

## 一、为什么会从"浏览器直接跑"演变到"工程化工具"

浏览器原生支持 ES Modules(`<script type="module">`)之后,理论上你完全可以不用任何工具,直接写 `import/export`,浏览器自己去请求依赖树。但这条路在生产环境行不通,原因有几个:

- **请求瀑布问题**:一个中大型应用几百上千个模块,浏览器要发几百个 HTTP 请求去逐个解析 import 语句、再发起下一批请求,即使有 HTTP/2 多路复用,延迟依然可观。
- **裸模块说明符(bare specifier)不被浏览器支持**:`import react from 'react'` 这种写法,浏览器不知道 `react` 对应哪个文件路径,必须靠打包工具做路径解析映射(或 import maps,但生态支持还不完整)。
- **语法降级需求**:TypeScript、JSX、装饰器、最新 ES 提案语法,浏览器不认识,必须编译。
- **产物优化**:Tree Shaking、代码分割(code splitting)、按路由懒加载、CSS/图片处理、压缩混淆,这些都不是"浏览器加载模块"能解决的。

所以"工程化工具"要解决的其实是两类完全不同的问题,这也是理解这张图的关键分野。

## 二、两个维度:打包器(Bundler) vs 编译器(Compiler)

图里把 Webpack/Vite/Rollup/Parcel/Rspack/esbuild/SWC 并列放在"工程化工具"下面,但它们不是同一维度的工具,而是**打包器**和**编译/转换引擎**两层,而且现实中是"打包器内部调用编译器"的组合关系:

| 类型 | 工具 | 作用 |
|---|---|---|
| 打包器(Bundler) | Webpack、Vite、Rollup、Parcel、Rspack | 做模块图分析、依赖解析、代码分割、产物输出策略 |
| 编译/转换引擎(Compiler) | esbuild、SWC、Oxc | 做单文件级别的语法转换(TS→JS、JSX→JS)、压缩、AST 操作,速度是核心卖点 |

也就是说,esbuild 和 SWC 严格说不是 Webpack/Vite 的"平级竞品",它们更像是被前者当作底层引擎调用的部件。这也是为什么现实中出现了大量"缝合"关系:

- **Vite(旧架构,7 及以前)**:开发环境用 esbuild 做依赖预构建(把 CommonJS 依赖转成 ESM、做极速转译),生产构建用 Rollup 打包(因为 Rollup 的 Tree Shaking 和产物质量更成熟)。这就导致 Vite 长期存在"dev 和 prod 用两套引擎、两套配置项(esbuild 选项 + rollupOptions)"的分裂问题,偶尔出现"dev 能跑、prod 报错"的诡异 bug。
- **Parcel**:内部用 SWC 做转换。
- **Next.js**:用 SWC 替换了 Babel 做编译(转译速度提升明显),但打包这一层过去仍是 Webpack,现在在往 Turbopack(Rust 写的)迁移。
- **Rspack**:定位是"Webpack 配置兼容 + Rust 重写内核",本质上是想让存量 Webpack 项目零成本或低成本迁移换取速度,内部转换层也用 SWC。

## 三、2026 年的最新变化:Rolldown 统一了 Vite 的双引擎

这里有个值得更新的信息:Vite 团队在 2025 年底到 2026 年推进了一个大动作——**Rolldown**(Rust 写的、Rollup 兼容 API 的打包器),现在已经成为 Vite 的默认打包器。

Vite 现在用 Rolldown 作为打包器,取代了之前 esbuild 和 Rollup 的组合。这意味着开发服务器和生产构建现在共用完全相同的模块解析和转换路径,"dev 能跑、prod 出问题"这类 bug 会大幅减少。Vite 8 于 2026 年 3 月正式稳定发布,Rolldown 成为所有 Vite 用户底层使用的打包器,5 月 Rolldown 自身也迎来 1.0 稳定版。

而且 Rolldown 底层还搭配了一套新的 Rust 工具链 **Oxc**(解析器、转换器、resolver、压缩器),这让 Vite 打包器、编译器都出自同一个团队维护,行为一致性更好,也能更快跟进最新的 ECMAScript 提案。速度上,Rolldown 比 Rollup 快 10 到 30 倍,项目越大差距越明显,性能已经和 esbuild 处于同一水平。

这个变化其实印证了整个生态的一个大趋势:**打包/编译核心逐渐从 JS 实现迁移到系统语言(Rust/Go)实现**。

## 四、性能差异的本质:JS 实现 vs 系统语言实现

这是理解 esbuild、SWC 为什么"快一个数量级"的核心原因,不只是"优化得好":

- **Webpack、Rollup(传统)、Parcel(早期)** 是 Node.js/JS 写的。JS 是解释执行(或 JIT),单线程为主(虽然可以用 worker 但通信开销大),AST 遍历、字符串处理这些高频操作在 JS 里天然比编译型语言慢。
- **esbuild** 是 Go 写的,天然支持 goroutine 并发,把解析、转换、打包这些阶段并行化;而且它用手写的高性能 parser,不像 Babel 那样走"插件化 AST 遍历"的通用架构,牺牲了灵活性换取速度。
- **SWC、Rspack、Rolldown/Oxc** 是 Rust 写的,零成本抽象 + 无 GC 停顿 + 更细粒度的并行,理论上限比 Go 版本还高一点,而且 Rust 的所有权模型让"多线程安全地共享/修改 AST"更容易做对。

这也是为什么 Babel(纯 JS、插件生态最丰富但最慢)在很多场景被 SWC/esbuild 替换掉——用可插拔性换速度,现在反过来用速度换来生态收敛。

## 五、逐个工具的定位和取舍

**Webpack**:生态最庞大、loader/plugin 体系最成熟,复杂场景(微前端 Module Federation、老项目历史包袱)兼容性最强,但配置复杂、构建慢,是"能做几乎所有事但要花更多心智负担"的选择。

**Vite**:开发体验的分水岭——利用浏览器原生 ESM + 编译引擎做按需转换,而不是先打包全部再服务,所以冷启动和 HMR 极快。2026 年之后随着 Rolldown 集成,生产构建速度也补齐了短板。

**Rollup**:Tree Shaking 的鼻祖和标杆,产物干净,历史上是"打库(library)优先"的选择,应用打包能力相对弱一些(路由懒加载、多入口这些应用级需求处理没有 Webpack 灵活),这也是当年 Vite 生产环境选它而不是自己重写打包器的原因。

**Parcel**:零配置理念的代表,开箱即用,但深度定制能力和生态不如 Webpack/Vite。

**Rspack**:字节跳动主导,目标是"Webpack 用户零成本换 Rust 内核",适合存量大型 Webpack 项目想提速又不想重写配置的场景。

**esbuild**:定位是"极速构建引擎",单独用可以做打包(简单场景够用),更多是被集成进别的工具里做转换/压缩这层。局限是历史上对某些复杂转换(比如某些装饰器提案、复杂的 Tree Shaking 策略)支持不如 Rollup/SWC 完整。

**SWC**:定位是"编译器",目标对标 Babel,做语法降级/转换,Next.js、Parcel、Rspack 都拿它当转换层用。

## 六、给你的落地建议

以你现在做的 Tauri + Vue 3 桌面工具、Three.js demo、Supabase 全栈项目这类场景,现实选型基本可以这样想:

- **应用开发(Vue/React)**:Vite 8(内置 Rolldown)基本是默认最优解,不用再操心 esbuild/Rollup 双配置的问题。
- **库(npm package)打包**:Rollup(或直接用 Rolldown standalone,兼容 Rollup API,速度快很多)。
- **存量超大型 Webpack 项目**:优先看 Rspack 做平迁移,而不是重写成 Vite。
- **单文件脚本/CLI 工具/简单场景**:esbuild 直接用,简单直接。

