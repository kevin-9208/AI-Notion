# 渲染进程主线程调度机制深入（面试高频考点）

这块是前端面试的重灾区，因为概念多且容易混淆（Event Loop、宏任务、微任务、渲染时机、rAF、rIC）。我按"从底层机制到实际调度顺序"的逻辑展开，最后给几道面试真题的标准答案。

## 一、先明确：主线程到底要干几件事

渲染进程主线程是**单线程**，但要处理五类工作，靠 Event Loop 来轮流调度：

1. 执行 JS（同步代码、回调）
2. 处理用户交互事件（点击、滚动、输入）
3. 执行定时器回调（setTimeout/setInterval）
4. 执行 Layout / Paint（渲染流水线）
5. 处理网络请求回调（fetch/XHR 的响应）

这些工作全部要挤过同一条主线程的"独木桥"，Event Loop 就是那个决定"谁先谁后"的调度器。

## 二、Event Loop 的核心结构

```
┌────────────────────────┐
│      调用栈 (Call Stack)  │  ← 同步代码在这里执行
└────────────────────────┘
            │  栈空后
            ▼
┌────────────────────────┐
│   微任务队列 (Microtask)  │  ← Promise.then、MutationObserver、queueMicrotask
└────────────────────────┘
            │  清空所有微任务后
            ▼
┌────────────────────────┐
│   宏任务队列 (Macrotask)  │  ← setTimeout、setInterval、I/O、UI事件、消息通道
│   （浏览器叫 Task Queue） │
└────────────────────────┘
            │
            ▼
      （可能触发渲染）
            │
            ▼
        回到调用栈
```

**一轮 Event Loop 的标准流程**：

```
1. 从宏任务队列中取一个任务执行（每轮只取一个）
2. 执行过程中产生的微任务，全部加入微任务队列
3. 当前宏任务执行完，调用栈清空
4. 清空微任务队列（注意：是"清空"，不是取一个）
   —— 而且执行微任务过程中如果又产生了新微任务，会继续加进队列被执行，
      直到队列真正清空为止
5. 判断是否需要渲染（不是每轮都渲染）
6. 如果需要渲染，走 Layout → Paint → Composite
7. 回到第1步，取下一个宏任务
```

**这是面试最容易考的一点：微任务队列是"清空式"消费，宏任务队列是"一次一个"消费。**

## 三、经典面试题：执行顺序问题

```javascript
console.log('1');

setTimeout(() => console.log('2'), 0);

Promise.resolve().then(() => console.log('3'));

console.log('4');
```

**输出：`1 4 3 2`**

解析：
1. 同步代码先跑：打印 `1`，然后 `4`
2. `setTimeout` 回调是宏任务，进宏任务队列排队
3. `Promise.then` 回调是微任务，进微任务队列
4. 当前宏任务（整段脚本本身就是第一个宏任务）执行完，栈空
5. 清空微任务队列：打印 `3`
6. 取下一个宏任务：打印 `2`

进阶版（微任务嵌套微任务）：

```javascript
Promise.resolve().then(() => {
  console.log('A');
  Promise.resolve().then(() => console.log('B'));
});
Promise.resolve().then(() => console.log('C'));
```

**输出：`A C B`**

因为微任务是逐个从队列取出执行的，A 执行时产生的新微任务 B 会被加到队列**末尾**，此时 C 已经排在 B 前面了，所以顺序是 A → C → B。这个题经常用来考察"是否真的理解微任务队列是 FIFO，而不是嵌套的立即执行"。

## 四、渲染时机：什么时候才会触发一次渲染？

这是比宏任务/微任务更进阶、也更容易被忽略的一层。**不是每个宏任务执行完都会渲染**，浏览器有自己的节流策略：

- 浏览器会尽量控制在 **60fps**（约每 16.6ms 一帧），如果时间没到，不会强制渲染
- 渲染这一步在**微任务队列清空之后**、**下一个宏任务开始之前**这个时间窗口执行
- 如果一个宏任务本身耗时很长（比如一段死循环式的 JS 计算），渲染会被推迟，因为渲染必须等主线程空闲

一个经典验证：

```javascript
document.body.style.background = 'red';
Promise.resolve().then(() => {
  document.body.style.background = 'blue';
});
```

结果是页面**只会显示蓝色**，不会先闪一下红色——因为两次样式修改都在同一个宏任务+微任务的执行周期内完成，浏览器只在这一切都结束后才真正渲染一次，微任务不会触发额外的渲染节点。

## 五、requestAnimationFrame（rAF）—— 渲染前的最后一站

rAF 不是宏任务也不是微任务，它是浏览器**专门为动画设计的 API**，回调会在**下一次重绘之前**执行，也就是精确插在"主线程空闲 → 即将 Paint"这个节点：

```
宏任务 → 微任务清空 → requestAnimationFrame回调 → Layout/Paint/Composite
```

用 rAF 做动画的核心优势：**和浏览器渲染节奏对齐**，避免用 setTimeout(fn, 16) 这种方式因为主线程繁忙导致的丢帧、掉帧问题。而且页面不可见（比如切到后台Tab）时，rAF 会自动暂停，省电省性能，setTimeout 则不会自动停。

## 六、requestIdleCallback（rIC）—— 捡空闲时间的活

这个是完全不同的调度维度，用于**低优先级任务**：

```javascript
requestIdleCallback((deadline) => {
  while (deadline.timeRemaining() > 0 && tasksLeft.length > 0) {
    doWork(tasksLeft.pop());
  }
}, { timeout: 2000 });
```

- 回调在**一帧的空闲时间**（渲染完成后，距下一帧还有余量时）才被执行
- `deadline.timeRemaining()` 告诉你这一帧还剩多少毫秒可以用，随时可能归零，需要写成可中断的分片任务
- `timeout` 参数：如果一直没有空闲时间，超时后**强制执行**，防止任务被无限饿死
- 典型应用场景：React Fiber 的可中断渲染理念、埋点上报、非关键 DOM 操作预处理

**注意**：rIC 不保证每帧都会被调用，如果主线程一直很忙（长时间没有空闲），回调可能被推迟很久，所以不适合做任何时间敏感的事情。

## 七、四种调度方式的优先级对比（面试常考表格）

| API | 类型 | 执行时机 | 典型用途 |
|---|---|---|---|
| `Promise.then` / `queueMicrotask` | 微任务 | 当前宏任务结束后立即执行，早于渲染 | 需要尽快执行、不希望被渲染打断的逻辑 |
| `setTimeout(fn, 0)` | 宏任务 | 下一轮 Event Loop，可能被渲染插队 | 延迟执行、打断长任务 |
| `requestAnimationFrame` | 特殊（渲染前） | 下一次重绘之前，与刷新率对齐 | 动画、视觉更新 |
| `requestIdleCallback` | 特殊（帧空闲期） | 当前帧渲染完，有空闲时间才执行 | 低优先级任务、不阻塞渲染的工作 |

## 八、消息通道（MessageChannel）—— 一个进阶知识点

`setTimeout(fn, 0)` 实际上有个坑：浏览器规定嵌套超过 5 层的 setTimeout，最小延迟会被强制拉到 4ms，这在需要"尽快但不阻塞渲染"的场景下不够精确。

React 的调度器（Scheduler）内部就是用 **MessageChannel** 来模拟一个没有最小延迟限制的宏任务，实现更精细的任务分片调度——这也是 React Fiber 能做到"可中断渲染"的底层机制之一，如果你被问到 React 调度原理，这是一个加分点。

## 九、几道高频追问及简答思路

**Q: 为什么 Promise.then 要比 setTimeout 先执行？**
> 因为 ECMAScript 规范把 Promise 定义为微任务，微任务队列在当前宏任务结束后立即、完整地清空，而 setTimeout 是宏任务，必须等到下一轮 Event Loop 才会被取出执行，中间还可能被渲染、其他优先级更高的处理插队。

**Q: async/await 和 Promise 的关系体现在 Event Loop 上是什么？**
> `await` 后面的代码本质上是被包装进 `.then()` 的微任务回调，`await` 之前是同步执行，遇到 `await` 会让出主线程（把后续代码丢进微任务队列），继续执行外部同步代码，等微任务轮到时再恢复。

**Q: 为什么长任务（Long Task，超过50ms的JS执行）会导致页面卡顿？**
> 因为渲染必须等主线程空闲才能执行（Layout/Paint 也跑在主线程），一个长时间占用调用栈的同步任务会持续阻塞渲染管线，同时也阻塞用户输入事件的响应，造成掉帧和"假死"体感。

**Q: Node.js 的 Event Loop 和浏览器的有什么不同？**
> Node 的 Event Loop 分成多个阶段（timers、pending callbacks、poll、check、close callbacks），每个阶段有自己的队列，且 `process.nextTick` 优先级比普通 Promise 微任务还高；浏览器则相对简单，只有宏任务/微任务两级，且多了"渲染时机"这个浏览器独有的调度点，这是 Node 环境完全没有的概念（因为 Node 不需要渲染 UI）。

---
